import React from 'react';

import {MovieDetails} from "../components";
const MoviePage = () => {
    return (
        <div>
            <MovieDetails/>
        </div>
    );
};

export {
    MoviePage
}